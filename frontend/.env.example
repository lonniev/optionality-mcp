# Optionality frontend — environment template.
# Copy to .env (untracked) and adjust for your deployment.

# MCP endpoint URL. For local dev, use the operator's local server.
# For production, point at the FastMCP Cloud deployment.
VITE_MCP_URL=http://localhost:8000/mcp
